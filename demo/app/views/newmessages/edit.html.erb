

<%= form_for @newmessage, :url => newmessage_path(@newmessage),:method =>:patch do |f| %>
<!-- { :controller => 'events', :action => 'update', :id => @event } -->
<%= form_for setup_newmessage(@newmessage), :url => event_path(@newmessage), :method => :put do |f| %>

  <%= render :partial => "form", :locals => {:f => f} %>

    <%= f.submit "Update", :class => "btn btn-primary" %>
<% end %>
<% end %>